# Project Setup

This project uses `asdf` for version management and `direnv` for environment variables.
Always source the environment with `source .envrc` and ensure `$HOME/.asdf/shims` is in PATH before running commands.
